# Utility functions shared across notebooks.
#
# NOTE: kept intentionally empty. The notebooks in `notebooks/` are published
# as standalone Kaggle kernels, where this `src/` package is NOT available.
# They must therefore remain self-contained — do not move notebook code here,
# or the Kaggle kernels will break on import. Use this module only for helpers
# consumed by local-only tooling (e.g. scripts/), never imported by notebooks.
